# Socratix screenshots

Replace these placeholder images with real captures from your running app:

1. **screenshot_diagnostic.png** — Chat during Socratic diagnostic mode (status banner shows "Diagnosing: …").
2. **screenshot_graph.png** — Sidebar Pyvis graph with mixed node colors (green/red/orange/gray).
3. **screenshot_teaching.png** — Chat in teaching mode after a misconception was detected.

To capture: run `python -m streamlit run app.py`, walk through a session, use Win+Shift+S (or your OS screenshot tool), save PNGs here with the names above.
