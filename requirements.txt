# Socratix dependencies (cumulative).
# Phase 10 will pin the full final set in one block; until then each phase
# appends its additions in order so partial installs stay reproducible.

# Phase 1 - concept graph + visualization
networkx==3.3
pyvis==0.3.2

# Phase 2 - local LLM via Ollama REST API (http://localhost:11434)
requests==2.32.3
