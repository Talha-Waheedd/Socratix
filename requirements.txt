# Socratix — pinned dependencies (final stack)
#
# Python 3.10+ required. Tested on 3.14.
# chromadb>= and streamlit>= use flexible pins because chromadb==0.5.5
# requires numpy<2 and may fail to install on Python 3.14+.

networkx==3.3
pyvis==0.3.2
requests==2.32.3
chromadb>=0.5.5
sentence-transformers>=3.0.1
streamlit>=1.39.0
